spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/best_travel'
    username: olvera
    password: olvera1
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder:
                TRACE
